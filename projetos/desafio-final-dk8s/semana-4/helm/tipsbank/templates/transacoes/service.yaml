apiVersion: v1
kind: Service
metadata:
  name: api-transacoes
  namespace: tipsbank-transacoes
  labels:
    {{- include "tipsbank.labels" . | nindent 4 }}
    app: api-transacoes
spec:
  selector:
    app: api-transacoes
  ports:
    - name: http
      port: 8080
      targetPort: 8080
