apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-transacoes
  namespace: tipsbank-monitoring
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - tipsbank-transacoes
  selector:
    matchLabels:
      app: api-transacoes
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
